===================
2023-03-20 13:37:27
===================

001. LEMBRAR DE SALVAR AS QUERIES ANTES
002. LEMBRAR DE EXPORTAR OS SETTINGS ANTES

(
cd %APPDATA%\JetBrains
del "PermanentDeviceId"
del "PermanentUserId"
rmdir "consentOptions" /s /q
)


(
cd %APPDATA%\JetBrains\PhpStorm2021.1
rmdir "eval" /s /q
del "options\other.xml"
)


(
cd %APPDATA%\JetBrains\DataGrip2021.1
rmdir "eval" /s /q
del "options\other.xml"
)

reg delete "HKEY_CURRENT_USER\Software\JavaSoft\Prefs\jetbrains"

